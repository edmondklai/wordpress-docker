A project on running word press using docker configuration

To run the website, clone the repository, in your terminal inside the project directory, run:

`docker-compose up -d`

access the site at localhost
